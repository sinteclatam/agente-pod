apiVersion: v1
kind: ServiceAccount
metadata:
  name: agente-pod
  namespace: agentes
# Si el paquete de GHCR es privado, crea el secreto de descarga y descomenta:
#   kubectl -n agentes create secret docker-registry ghcr \
#     --docker-server=ghcr.io --docker-username=<usuario> --docker-password=<token>
# imagePullSecrets:
#   - name: ghcr
